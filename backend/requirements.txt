fastapi==0.115.5
uvicorn[standard]==0.32.1
numpy==2.1.3
requests==2.32.3
apscheduler==3.10.4
global-land-mask==1.0.0
weasyprint==63.0
python-dotenv==1.0.1
Pillow==10.4.0
pypdf==6.10.2
# Typst PDF engine — Rust-backed manylinux wheel, no system deps.
# Selected via HAVOC_PDF_ENGINE=typst; WeasyPrint stays default
# during the migration coexistence window (see migration plan).
typst==0.14.8
